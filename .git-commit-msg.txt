fix(seo): audit complet — robots, manifest, og-image, favicon, SeoHead

- Supprime public/index.html (doublon obsolète qui pouvait être servi en brut)
- robots.txt : retire le Disallow /*.json (bloquait manifest.json) ; ajoute
  règles ciblées pour checkout/panier/notifications et paramètres dynamiques
- Crée public/favicon.svg + public/og-image.svg (assets manquants jusque-là)
- manifest.json réduit aux icônes existantes (SVG vectoriel scalable)
- index.html : favicon SVG, og:image SVG, og:locale fr_CM + alternates,
  twitter:site/creator, og:image:alt, SearchAction format EntryPoint,
  Organization.logo → /favicon.svg, Organization.image → /og-image.svg
- SeoHead : og:image SVG par défaut, og:image:alt/type dynamiques,
  twitter:site/creator, hreflang fr-CM, googlebot mirroré
- browserconfig pointe vers favicon.svg
